<script setup lang='ts'>
  import { Button, Table } from '@/components/ui'
</script>

<template>
  <div class='space-y-4'>
    <div class='flex justify-between'>
      <h1 class='text-xl font-bold'>{{className}}</h1>
      <Button to='/{{moduleName}}/create'>Novo</Button>
    </div>

    <Table :columns='[]' :data='' has-actions density='compact' variant='elevated'>
      <template #actions='{ row }'>
        <Button variant='secondary' to='`/{{modulename}}/${row.id}/edit`'> Editar </Button>
      </template>
    </Table>
  </div>
</template>